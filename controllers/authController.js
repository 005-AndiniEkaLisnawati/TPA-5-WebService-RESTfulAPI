module.exports = {
    register: (req, res) => {
        try {
            const {name, email, password} = req.body;
        if(!name|!email|!password){
            
        }

        } catch (e) {
            
        
        
        }
    },
    login: (req, res) => {

    }
}